public enum DialogEvent {
    PRE_MOVE,
    POST_MOVE,
    WIN,
    LOSE,
    TIE,
    BLOCK,
    ILLEGAL_MOVE
}
//
////    PRE_MOVE = Before player makes it move
////    POST_MOVE = After the player makes it move
////    WIN = player Wins
////    LOSE = player Lose
////    TIE = When it's a Tie
////    BLOCK = Player blocking its opponent
////    ILLEGAL_MOVE = layer try to make a illegal move
//
